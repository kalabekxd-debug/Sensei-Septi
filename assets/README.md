# Image Assets

Upload the actual images into this folder using these exact filenames:

- `teacher.webp` — portrait of Septi M. S.pd
- `creator.webp` — portrait of the website creator

The HTML references these files with relative paths, so after deployment through Vercel they are publicly served from `/assets/...` and can be viewed from other devices.